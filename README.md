# ✋ Hand Gesture Recognition Using Machine Learning



## 📝 Overview

This project focuses on developing a **Hand Gesture Recognition** system using computer vision and machine learning techniques. The goal is to build a robust model that can interpret and classify human hand gestures from images, enabling their use in real-time applications.

---

## ✨ Problem Statement

The core problem addressed is the classification of hand gesture images into distinct, predefined categories.

---

## 🎯 Objectives

The successful completion of this project involved achieving the following key objectives:

* **Create a robust hand gesture dataset.**
* **Preprocess** the collected images for model readiness.
* **Train** an appropriate Machine Learning/Deep Learning model.
* **Evaluate** the model's performance and accuracy.
* **Deploy** the final system for **real-time** recognition.

---

## 🛠️ Methodology

The project followed a standard Machine Learning development pipeline:

1.  **Data Collection:** Gathering a sufficient number of images representing various hand gestures.
2.  **Preprocessing:** Standardizing the input by **resizing** and **normalizing** the images.
3.  **Feature Extraction:** Using the layers of a **Convolutional Neural Network (CNN)** to automatically extract salient features from the image data.
4.  **Training:** Training the CNN model using the **TensorFlow** framework.
5.  **Evaluation:** Assessing the model's performance using metrics such as **Accuracy** and **F1 Score**.
6.  **Deployment:** Setting up the final system for **real-time** gesture recognition via a **webcam**.

---

## 💻 Libraries Used

This project was built using the following key Python libraries:

* **OpenCV:** For image and video processing, particularly for real-time webcam input.
* **NumPy:** For efficient numerical operations and data handling.
* **TensorFlow:** The primary framework used for building and training the Deep Learning model (CNN).
* **Scikit-learn:** For machine learning utilities, including evaluation metrics.
* **Matplotlib:** For data visualization, such as plotting training loss and accuracy.

---

## ✅ Results and Conclusion

### Results

The trained **Convolutional Neural Network (CNN)** model achieved **high accuracy** in the task of gesture classification.

### Conclusion

The developed model successfully recognizes and classifies hand gestures. This system is ready to be utilized in **real-time applications** where human-computer interaction can be enhanced through non-contact hand movements.

---